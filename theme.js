import Provider from "./components/Provider";

export const theme = {
  // add a custom font
  font: "Roboto, sans-serif",
  Provider,
  // custom colors
  colors: {
    text: "#fff",
    background: "#663399",
    link: "#fff"
  }
};
